This directory intentionally contains no application assets.

Firebase Hosting rewrites every request to the Capital Aperture Cloud Run
service so the React application and its same-origin API remain one release.
